Resolved layout boxes drive effective clips, hit bounds, and visual diagnostics consistently, and the complete layout matrix is protected by snapshots.

## Scope

- Compute content/padding/clip rectangles after layout.
- Propagate clip intersections into render and input stages.
- Add debug overlays for measured, resolved, content, and clip boxes.
- Create aspect, scale, text-change, visibility, overflow, and nested-layout scenarios.

## Acceptance criteria

- [ ] Render clip and hit-test clip agree on visible interactive area.
- [ ] Debug overlays do not alter layout or batch ordering.
- [ ] Layout errors include a node path and relevant constraints.
- [ ] Snapshot updates require explicit approval.
- [ ] No scenario depends on DOM layout.

## Verification

- `pnpm run test:layout`
- `pnpm run visual:layout -- --verify`

## Evidence

- Commit layout report HTML and JSON snapshots.

## Out of scope

- Arbitrary mask debugging.
- Browser devtools integration.

## Dependencies

- Blocked by `HUD-030`
- Blocked by `HUD-046`
- Blocked by `HUD-047`
- Blocked by `HUD-048`
- Blocked by `HUD-049`

## Local brief

`planning/tickets/E07/HUD-050-integrate-clipping-propagation-layout-diagnostics-and-regression-matrices.md`
