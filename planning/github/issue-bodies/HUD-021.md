Layouts can target a safe logical frame, inspect letterbox/crop regions, and apply user HUD scaling independently from the game camera or browser DPR.

## Scope

- Support explicit logical safe insets and an optional host safe-area adapter.
- Expose content, safe, and visible rectangles.
- Apply HUD zoom around a documented anchor.
- Define clamping and diagnostics for invalid zoom.

## Acceptance criteria

- [ ] Safe-frame anchors resolve against the safe rectangle when configured.
- [ ] HUD zoom changes layout/render scale without changing host renderer pixel ratio.
- [ ] Letterbox rectangles are available to debug overlays.
- [ ] Zoom changes invalidate only required viewport/layout/render stages.

## Verification

- `pnpm run test -- safe-frame hud-zoom`

## Evidence

- Capture safe-frame and zoom lab screenshots.

## Out of scope

- Reading CSS environment variables in core.
- User preference persistence.

## Dependencies

- Blocked by `HUD-018`
- Blocked by `HUD-019`

## Local brief

`planning/tickets/E03/HUD-021-add-safe-insets-letterbox-bounds-and-independent-hud-zoom.md`
